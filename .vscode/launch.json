{
    "version": "0.3.0",
    "configurations": [
        {
            "name": "Debug mesh-to-inp",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/mesh_to_inp/cli.py",
            "console": "integratedTerminal",
            "args": [
                "${workspaceFolder}/sim_config.yaml"
            ]
        }
    ]
}