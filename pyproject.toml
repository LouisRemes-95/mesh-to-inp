[project]
name = "mesh-to-inp"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "meshio>=5.3.5",
    "pyyaml>=6.0.3",
    "rich>=14.3.3",
]

[project.scripts]
mesh-to-inp = "mesh_to_inp.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
